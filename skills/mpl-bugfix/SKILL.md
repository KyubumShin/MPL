---
description: "[DEPRECATED] Standalone bug fixing — use /mpl:mpl instead (auto-routes via pp_proximity). Triggers on: 'mpl bugfix', 'mpl fix', 'bug 고쳐', 'mpl 버그'."
---

# MPL Bugfix (Deprecated)

This skill has been integrated into `/mpl:mpl` (v2.0). The unified entry point auto-detects task complexity and routes via Hat level and pp_proximity classification.

**To use**: run `/mpl:mpl` with your bug description. Add `bugfix` or `fix` keyword to hint non_pp proximity (lightweight Hat), or let auto-scoring decide.
