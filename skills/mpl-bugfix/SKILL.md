---
description: "[DEPRECATED] Standalone bug fixing — use /mpl:mpl instead (auto-routes to frugal tier via F-20). Triggers on: 'mpl bugfix', 'mpl fix', 'bug 고쳐', 'mpl 버그'."
---

# MPL Bugfix (Deprecated)

This skill has been integrated into `/mpl:mpl` (v3.2). The unified entry point auto-detects task complexity and routes to the appropriate tier.

**To use**: run `/mpl:mpl` with your bug description. Add `bugfix` or `fix` keyword to hint frugal tier, or let auto-scoring decide.
